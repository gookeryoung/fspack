[project]
name = "$project_name"
version = "0.1.0"
description = "$description"
requires-python = ">=3.8,<3.12"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.fspack]
web-static-dirs = ["frontend"]
