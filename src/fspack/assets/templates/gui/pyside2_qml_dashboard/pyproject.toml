[project]
name = "pyside2_qml_dashboard"
version = "0.1.0"
description = "PySide2+QML 仪表盘：Qt 声明式界面、QML 资源收集与 uv.lock 真实项目形态"
readme = "README.md"
requires-python = ">=3.8,<3.11"
dependencies = [
    "pyside2>=5.15.2.1",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
