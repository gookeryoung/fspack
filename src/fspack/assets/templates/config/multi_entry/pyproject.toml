[project]
name = "multi_entry"
version = "0.1.0"
description = "三入口项目（cli+gui+web）：[project.scripts] 多入口声明，验证按入口产出与运行验证"
requires-python = ">=3.8,<3.11"
dependencies = ["PySide2>=5.15.2", "flask"]

[project.scripts]
cli = "cli:main"
gui = "gui:main"
web = "web:main"
